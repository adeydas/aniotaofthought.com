box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.37"
        theme: kiss
        config: config.toml
deploy:
	steps:
	 - ClusterVR/wercker-step-s3sync:
	 	key:    $AWS_ACCESS_KEY
	 	secret: $AWS_SECRET_ACCESS_KEY
	 	source: $SOURCE_PATH
	 	bucket: $DESTINATION_BUCKET